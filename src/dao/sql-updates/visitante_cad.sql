CREATE TABLE visitante_cad (
cpf varchar(12) PRIMARY KEY NOT NULL, rg varchar(12), nome varchar(50), email varchar(20), telefone varchar(20),
login varchar(20) NOT NULL, senha varchar(20) NOT NULL, datanasc DATE);